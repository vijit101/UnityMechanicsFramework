Add your Quest System walkthrough video here (e.g. QuestSystemTutorial.mp4) per CONTRIBUTING.md.

Suggested coverage for the current sample:
- Press Play in `Assets/Scenes/DemoScene.unity` (single `QuestDemo` object; world + UI spawn at runtime).
- Walk the 3D demo: goblin camp (F), loot, merchant ring (E), iron ore, East Gate / North Tower zones.
- Show quest tracker + event log + reward feed updating live.
- Save (S), reload scene or continue, Load (L) to restore quest progress, player position, and consumed entities.
- Link the hosted URL in the root README Mechanics Library Quick Navigation row for Quest / Objective System.
